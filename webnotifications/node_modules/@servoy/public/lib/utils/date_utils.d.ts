import { DateTime } from 'luxon';
/**
 * Returns a zero-based index for first day of the week, as used by the specified locale
 * e.g. Sunday (returns 0), or Monday (returns 1)
 *
 * @param locale
 * @returns
 */
export declare const getFirstDayOfWeek: (locale: string) => number;
/**
 * Floors the specified date to the beginning of week.
 *
 * @param date
 * @returns
 */
export declare const floorToWeek: (date: DateTime) => DateTime;
